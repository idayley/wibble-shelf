**What it is:** <!-- one sentence -->

- [ ] My repo is public.
- [ ] `ref` is the full 40-character commit hash, not a branch or tag.
- [ ] `id` and `version` match my `wibble.json` (extensions and pins).
- [ ] It only asks for the capabilities it uses.
- [ ] I tried it in Wibble (for a toy, say so and the reviewer will).
- [ ] For an update: I added a `history` line saying what changed.
